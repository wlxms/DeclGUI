namespace DeclGUI.Core
{
    /// <summary>
    /// 伪类枚举定义
    /// </summary>
    public enum PseudoClass
    {
        Normal,    // 默认状态
        Hover,     // 鼠标悬停
        Active,    // 激活状态（如按钮按下）
        Focus,     // 获得焦点
        Disabled   // 禁用状态
    }
}